{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set granted to the main LensLM window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
