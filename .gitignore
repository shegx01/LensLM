# Rust / Cargo
/target
**/target

# Node / Bun
node_modules

# SvelteKit
.svelte-kit
/build

# Tauri generated schemas
src-tauri/gen/schemas

# Test artifacts
/test-results
/playwright-report
/playwright/.cache
/coverage
# Stray LanceDB stores some engine tests write to a cwd-relative path.
lens-core/lancedb/
src-tauri/lancedb/

# OS
.DS_Store

# Icon generation source (regenerated on demand)
/app-icon.png
.omc/

# Git worktrees (feature work is isolated in worktrees; never tracked)
worktrees/

# Vendored pdfium dylib (M4 Phase 2 PDF extractor). Fetched + checksum-verified
# via scripts/fetch-pdfium.sh; NEVER committed (the script + CHECKSUMS are).
src-tauri/frameworks/libpdfium.dylib
.omg
